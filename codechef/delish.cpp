#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    bool allpos = true;
    long n;
    cin >> n;
    long a[n];
    for(long i = 0; i < n; i++){
        cin >> a[i];
    }
    
}