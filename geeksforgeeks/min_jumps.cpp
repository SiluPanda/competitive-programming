#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main(){
    int tt;
    cin >> tt;
    while(tt--){
        ll n;
        cin >> n;
        ll a[n];
        for(ll i = 0; i < n; i++){
            cin >> a[i];
        }
        ll dp[n] = {INT_MAX};
        dp[n-1] = 0;
        for(ll i = n-2; )
    }
}