#include <bits/stdc++.h>
using namespace std;

struct node{
    
    int key;
    node* left;
    node* right;
};

int main(){
    node* nn
    nn->key = 2;
    nn->left = NULL;
    nn->right = NULL;
    cout << nn->key <<nn->left;
}